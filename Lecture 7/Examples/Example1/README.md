# Creating a Wi-Fi Access Point
This example demonstrates how to configure the ESP32-S3 as a Wi-Fi Access Point (AP) using ESP-IDF. In Access Point mode, the ESP32 creates its own wireless network, allowing phones, computers, and other Wi-Fi devices to connect directly without requiring an external router. 


